# Configuration MongoDB
# Pour le développement local, utilisez MongoDB Atlas (gratuit) ou installez MongoDB localement
MONGODB_URI=mongodb://localhost:27017/caesar
# Ou utilisez MongoDB Atlas :
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/caesar?retryWrites=true&w=majority

# JWT Secret - CHANGEZ CECI EN PRODUCTION ! Utilisez une chaîne aléatoire sécurisée
JWT_SECRET=votre_secret_jwt_tres_securise_changez_moi_en_production

# Port du serveur
PORT=5000

# URL du frontend (pour CORS)
FRONTEND_URL=http://localhost:3000
